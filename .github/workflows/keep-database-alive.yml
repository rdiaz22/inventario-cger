name: Keep Database Alive

on:
  schedule:
    - cron: '0 0 * * *'   # 00:00 UTC
    - cron: '0 12 * * *'  # 12:00 UTC
  workflow_dispatch: {}

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: scripts/package-lock.json

      - name: Install dependencies (scripts)
        working-directory: scripts
        run: npm ci

      - name: Run keep-alive
        working-directory: scripts
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
        run: npm run keep-alive

name: Keep Database Alive

on:
  schedule:
    # Ejecutar cada 12 horas (a las 00:00 y 12:00 UTC)
    - cron: '0 */12 * * *'
  
  # También se puede ejecutar manualmente desde GitHub
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: false
        default: 'info'
        type: choice
        options:
        - info
        - warning
        - error

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Create .env file
      run: |
        echo "VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }}" > .env
        echo "VITE_SUPABASE_ANON_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }}" >> .env
        
    - name: Run keep-alive script
      run: node scripts/keep-alive.js
      
    - name: Notify success
      if: success()
      run: |
        echo "✅ Base de datos mantenida exitosamente"
        echo "🕐 Ejecutado en: $(date -u)"
        
    - name: Notify failure
      if: failure()
      run: |
        echo "❌ Error al mantener la base de datos"
        echo "🕐 Error en: $(date -u)"
